// FoxQL — compile-time type-safe SQL builder for PostgreSQL.
//
// Public API re-exports from sub-packages.
// Users typically import jaredzhou/foxql/builder and jaredzhou/foxql/ast
// directly, or jaredzhou/foxql for the convenience surface.
