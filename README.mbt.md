# moonbase

This repository is organized as a small MoonBit workspace:

- `moonbase` is the reusable library package at the repository root.
- `cmd/main` is the runnable CLI that depends on the library.

Run the CLI with:

```bash
moon run cmd/main
```
